# 数据结构课程设计总结报告
## 题目 0：互联网视频直播高并发弹幕时间轴对齐缓存

- **姓名 / 学号**：____________________
- **专业 / 课程**：数据结构课程设计
- **开发平台**：Windows 11 + Node.js 18+ + Vite 5 + React 18（纯前端 HTML/CSS/JS），仅在性能压测脚本阶段使用 Node 运行 JS（无需 Python）
- **提交日期**：2026-09

---

## 一、软件功能与背景

在大型网络直播（电竞赛事、跨年晚会）中，精彩瞬间弹幕会爆发式增长（每秒数万条）。弹幕到达服务器的时间戳因网络抖动、基站切换、客户端重发而**严重乱序**。服务器必须在内存中建立**高效滑动的时序缓冲区**，完成三件事：

1. **原地插入排序**：把乱序到达的弹幕按"视频时间戳"插入到正确位置，保证整条时间轴有序；
2. **滑动窗口切除回收**：随着视频时间轴每秒前进 1 秒，自动切除并释放窗口（默认 30 秒）之前的过期弹幕，节点归还对象池而非 GC；
3. **权重限流降级**：当某秒弹幕密度超过阈值（默认 500 条/秒）时，优先丢弃权重低的弹幕。

本系统以**「带水平索引的双向循环链表」为目标方法**，以**「标准单链表」为基准方法**，在多种数据规模下做时间/空间性能对比，并提供可视化面板。

---

## 二、设计思想

| 设计要点 | 方案 |
|---|---|
| 节点复用 | 固定大小 **对象池（ObjectPool）**：释放的节点入自由链表，新弹幕优先从池取，避免高频 `new`/GC |
| 有序插入 | 手写**带水平索引的双向循环链表**：块级索引 → 秒级槽（slot）→ 100ms 子桶（sub-bucket）三级水平索引 |
| 滑动窗口 | 环形秒槽数组（长度取 2 的幂 128），`windowStart = nowSec - W + 1`，每秒 `evictBefore` 切除 |
| 限流降级 | 槽内缓存 `minWeight`，密度超阈值时高权重顶替最低权重者，否则丢弃新弹幕 |
| 基准对照 | 标准单链表仅维护秒级槽、无子桶、无 prev 指针，插入需从头线性扫描 |

**"水平索引"含义**：横向把时间轴切成「块（block）→ 秒（slot）→ 子桶（100ms）」三层。定位一条弹幕时，先 O(1) 落到秒槽，再在 10 个子桶内定位，把平均比较次数从 O(n) 降到近似 O(1)；双向指针使"摘除任意节点"也是 O(1)。这是典型的**以空间换时间**。

---

## 三、逻辑结构与物理结构

### 1. 节点（手工定义，非内置集合）
```js
// 双向循环链表节点（12 字段）
{ id, ts, seq, weight, vip, text, user, color, prev, next, up, sub }
// 标准单链表节点（10 字段，少 prev / up）
{ id, ts, seq, weight, vip, text, user, color, next, nextFree }
```
- `prev`/`next`：双向循环链表的双向指针；`up`：指向所在子桶头；`sub`：子桶下标。
- 所有节点均从 `ObjectPool` 获取/归还，物理上连续复用。

### 2. 水平索引（环形槽 + 块级索引）
- 环形槽数组 `slots[128]`：下标 `slotMask = 127`，`slotOf(sec) = sec & 127`，O(1) 定位秒槽。
- 每个秒槽内含 10 个 100ms 子桶（双链表），以及 `minWeight / minNode` 缓存。
- 块级索引 `blocks[16]`：每 8 秒一"块"，块头指向该块内首/尾活动槽，支撑"整块跳过"。

### 3. 对象池
- 自由链表 `freeHead` + `create/destroy` 计数；`acquire()` 取头，`release()` 还头；支持 `preload(n)` 预热（模拟服务端启动预分配）。

---

## 四、核心算法

### 算法 1：动态滑动窗口更新（每秒）
```
advance():
  nowSec++; cutoff = nowSec - W + 1;
  n = list.evictBefore(cutoff);   // 沿链表头依序切除 ts 落在窗口前的节点
  if n>0: 回收 n 个节点 → 归还对象池; 高亮日志 + 界面闪烁
```
`evictBefore` 利用链表的全局有序性，从头连续摘除过期段，无需回扫。

### 算法 2：乱序弹幕插入排序 + 限流降级（receive）
```
receive(d):
  sec = d.ts/1000;
  if sec > nowSec          → 拒绝（时间戳超前）
  if sec < windowStart     → 拒绝（已过期）
  node = pool.acquire(); node ← d;
  slot = index.slotOf(sec);
  if slot.count >= limitPerSec:          // 触发降级
      victim = findMinWeightInSec(sec);
      if d.weight > victim.weight: 顶替(victim 回收, d 入窗)   // 降级顶替
      else: 丢弃 d(回收), droppedLow++                         // 降级丢弃
  else: insertNode(node);                 // 定位到 sub-bucket 内正确位置插入
```

### 算法 3：单线程发射器（边界场景）
`Emitter` 每步生成一批弹幕（30% 为延迟 3~5 秒的老弹幕，制造乱序），并支持手动注入：
- **（1）老弹幕突发**：`injectLateBurst` 一次注入大量延迟 3~5s 的弹幕，测试乱序插入；
- **（2）空窗滑动**：`emitGap` 阶段若干秒零输入，窗口仍正常前推、旧数据回收；
- **（3）超承载丢弃**：`injectFlood` 单步注入超过上限，触发限流日志与闪烁。

---

## 五、功能交互可视化（实时面板）

| 面板 | 内容 |
|---|---|
| 直播画面 | 弹幕从右向左飘过；延迟到达的老弹幕加虚线下划线 |
| 密度柱状图 | 30 个时间槽，柱高=该槽弹幕条数，颜色随密度由浅到深，超阈值标红 |
| 窗口结构 | 选中某秒，展开该秒槽的链表节点列表（ts/权重/文本），展示结构细节 |
| 统计面板 | 收/插/丢弃/顶替/回收/拒绝等计数 + 对象池命中率 + 估算内存 |
| 日志控制台 | 回收/限流/顶替/拒绝/乱序突发分类高亮；发生时界面闪烁提示 |
| 性能曲线 | 两种结构在多种规模下的耗时/比较次数/内存曲线（图 1~3） |

---

## 六、性能测试结果与曲线分析

实验条件：窗口 30s，单秒上限 500，到达 300 条/秒，30% 弹幕延迟 3~5 秒，固定随机种子保证两种结构**输入序列完全一致**。

| 数据规模 | 目标耗时 ms | 基准耗时 ms | 时间比 | 目标比较次数 | 基准比较次数 | 比较比 | 目标内存 | 基准内存 |
|---|---|---|---|---|---|---|---|---|
| 1,000 | 1.61 | 2.63 | 1.6x | 7,353 | 324,042 | 44.1x | 112.3 KB | 86.6 KB |
| 3,000 | 2.18 | 13.13 | 6.0x | 26,309 | 3,648,109 | 138.7x | 299.8 KB | 242.9 KB |
| 6,000 | 2.03 | 65.99 | 32.5x | 54,839 | 15,938,889 | 290.6x | 581.0 KB | 477.3 KB |
| 10,000 | 3.81 | 260.09 | 68.2x | 92,711 | 45,042,582 | 485.8x | 834.1 KB | 688.2 KB |
| 20,000 | 4.34 | 688.70 | 158.7x | 186,052 | 125,196,142 | 672.9x | 834.1 KB | 688.2 KB |

**结论（结合曲线）**：
- **时间&比较次数**：基准（标准单链表）每次插入都从头线性扫描，比较次数随窗口内弹幕总量 n 线性增长（O(n)），耗时随规模急剧上升；目标方法靠三级水平索引把定位范围压到常数时间片，比较次数与 n 基本无关（近似 O(1)），且双向指针使摘除任意节点也是 O(1)。规模越大，差距越悬殊（时间最高 158.7x，比较最高 672.9x）。
- **空间**：目标方法每个节点多 2 个指针（prev/up）并维护槽/子桶索引，固定开销约 1.21x，属于典型"以空间换时间"。峰值结点数两种结构相同（受窗口与上限约束），故内存曲线在 8700 条后都趋于平台。

> 注：报告中的曲线图（图 1 耗时、图 2 比较次数、图 3 内存）由界面「性能对比实验」面板实时绘制，可在程序中复现；上述表格为相同实验的同一组数据。

---

## 七、系统安装、运行与操作

### 安装
```bash
npm install
```

### 运行（开发模式）
```bash
npm run dev        # 浏览器打开提示的 http://localhost:5173
```

### 构建 / 预览生产版
```bash
npm run build      # 产物在 dist/
npm run preview
```

### 命令行测试与压测（Node，无需 Python）
```bash
npm test           # 运行核心数据结构与边界场景单元测试
npm run bench      # 命令行打印上面的性能对比 Markdown 表格
npm run smoke      # 服务端渲染冒烟测试，确认 React 运行时无错
```

### 界面操作
1. 点「开始」自动按速度推进时间轴；或点「单步(+1s)」逐秒观察。
2. 切换「带水平索引的双向循环链表 / 标准单链表」对比二者在同等输入下的表现。
3. 「注入老弹幕突发」验证乱序插入；「注入流量洪峰」验证限流降级；日志与界面闪烁会高亮提醒。
4. 选不同「场景」（混合/平稳/突发/空窗）观察窗口结构与密度柱状图变化。
5. 展开「性能对比实验」点「开始实验」，多档规模下绘制时空曲线。

---

## 八、文件结构与源码说明

```
hype/
├── index.html                 入口
├── package.json               脚本：dev/build/test/bench/smoke
├── vite.config.js
├── readme.txt                 原始题目
├── src/
│   ├── main.jsx / App.jsx      React 应用与主控
│   ├── styles.css             全局样式（深色科技风）
│   ├── core/                  ★ 纯手工核心数据结构与算法（无内置集合库）
│   │   ├── ObjectPool.js          对象池（节点复用）
│   │   ├── nodes.js              节点工厂 + 内存估算常量
│   │   ├── HorizontalIndex.js    块/秒槽/100ms 子桶 三级水平索引
│   │   ├── IndexedCircularList.js 带水平索引的双向循环链表（目标方法）
│   │   ├── SinglyLinkedList.js    标准单链表（基准方法）
│   │   ├── DanmakuEngine.js       引擎：滑动窗口/插入/限流降级/日志
│   │   ├── emitter.js             单线程发射器与三种边界场景
│   │   └── benchmark.js           性能对比测试
│   ├── components/            可视化面板（弹幕舞台/密度/结构/统计/日志/性能）
│   └── ssr-smoke.jsx          服务端渲染冒烟测试入口
├── scripts/bench.mjs          命令行性能脚本
└── tests/core.test.mjs        核心单元测试（含三类边界场景）
```

**严禁使用内置集合库**：所有有序结构、索引、对象池均为手写指针操作；仅用普通数组做可视化采样与统计聚合。

---

## 九、小结

本项目完整实现了题目要求：手工构建了带水平索引的双向循环链表与对象池，实现了乱序插入、滑动窗口回收与权重限流降级，覆盖三类边界场景；并以标准单链表为基准，给出了多规模下的时间/空间实验数据与曲线，结合可视化面板直观呈现了数据结构选择对高并发弹幕系统的决定性影响。
